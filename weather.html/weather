<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Weather App</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="style.css" />
</head>
<body class="bg-cover bg-center min-h-screen text-white">
  <div class="overlay">
    <div class="flex items-center justify-center h-screen">
      <div class="bg-white bg-opacity-20 backdrop-blur-md rounded-lg p-8 shadow-xl w-full max-w-md text-center">
        <h1 class="text-3xl font-bold mb-6">Weather App</h1>
        <input
          id="cityInput"
          type="text"
          placeholder="Enter location"
          class="w-full px-4 py-2 rounded text-black mb-4"
        />
        <button
          onclick="getWeather()"
          class="bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded text-white w-full"
        >
          Search
        </button>
        <div id="weatherResult" class="mt-6 text-white text-lg"></div>
      </div>
    </div>
  </div>
  <script src="script.js"></script>
</body>
</html>